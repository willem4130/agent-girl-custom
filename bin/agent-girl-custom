#!/bin/bash
# Agent Girl Custom - Start development server

PROJECT_DIR="/Users/willemvandenberg/Dev/agent-girl-custom"

cd "$PROJECT_DIR" || exit 1

# Kill any existing process on port 3001
lsof -ti:3001 | xargs kill -9 2>/dev/null

# Start the dev server
exec bun run dev
